{
  "permissions": {
    "deny": [
      "Edit(.env*)",
      "Write(.env*)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '[aro knowledge check] セッション開始時の自動チェック結果:'; node /Users/makinokaedenari/yamk12nfu/ai-repo-ops/packages/aro-cli/bin/aro knowledge check --repo \"$CLAUDE_PROJECT_DIR\" 2>&1; exit 0",
            "timeout": 30,
            "statusMessage": "knowledge の鮮度を確認中…"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "command -v jq >/dev/null 2>&1 || { echo 'hooks: jq が見つからないため型チェックを実行できません(brew install jq)' >&2; exit 2; }; f=$(jq -r '.tool_input.file_path // \"\"'); case \"$f\" in *.ts|*.tsx) cd \"$CLAUDE_PROJECT_DIR\" && out=$(npx tsc --noEmit 2>&1) || { printf '%s\\n' \"$out\" | head -30 >&2; exit 2; } ;; esac",
            "timeout": 120,
            "statusMessage": "型チェック中(tsc --noEmit)…"
          }
        ]
      }
    ]
  }
}
